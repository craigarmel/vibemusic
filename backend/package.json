{
  "name": "vibemusic-backend",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": ".venv/bin/python -m uvicorn app.main:app --reload --reload-dir app --host 0.0.0.0 --port 8000",
    "start": ".venv/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port 8000"
  }
}
